<h1><%= @child.name %></h1>
<p><%= @child.age %></p>
<p><%= @child.school %></p>
<%= link_to "Update record", edit_user_child_path(current_user.id) %><br>
<%= link_to "Go Back", "/users/#{current_user.id}" %>

<h2>Bookings for <%= @child.name %></h2>

<%= month_calendar do |date| %>
  <%= date %>
<% end %>
